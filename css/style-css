/* Core Variables */
:root {
  --mint-green: #00d63f;
  --dark-bg: #0a0b10;
  --text-white: #ffffff;
  --text-gray: #9ca3af;
  --font-main: 'Plus Jakarta Sans', sans-serif;
}

/* Base Styles */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  background-color: var(--dark-bg);
  color: var(--text-white);
  font-family: var(--font-main);
  line-height: 1.5;
  overflow-x: hidden;
}

.container {
  width: 90%;
  max-width: 1200px;
  margin: 0 auto;
}

/* Navigation */
.header {
  padding: 20px 0;
  position: fixed;
  width: 100%;
  top: 0;
  z-index: 1000;
  background: rgba(10, 11, 16, 0.8);
  backdrop-filter: blur(10px);
}

.nav-wrapper {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.nav-link {
  color: var(--text-white);
  text-decoration: none;
  margin-left: 30px;
  font-weight: 500;
  transition: color 0.3s;
}

.nav-link:hover {
  color: var(--mint-green);
}

/* Hero Section */
.hero-section {
  padding: 160px 0 100px;
  text-align: center;
  background: radial-gradient(circle at top, #1a1c2e 0%, var(--dark-bg) 70%);
}

.display-h1 {
  font-size: clamp(3rem, 8vw, 5rem);
  font-weight: 800;
  line-height: 1;
  margin-bottom: 20px;
}

.display-h1._02 {
  color: var(--mint-green);
}

.hero-subtitle {
  color: var(--text-gray);
  max-width: 600px;
  margin: 0 auto 40px;
  font-size: 1.2rem;
}

/* Buttons */
.button-primary {
  background-color: var(--mint-green);
  color: #000;
  padding: 14px 28px;
  border-radius: 50px;
  text-decoration: none;
  font-weight: 700;
  transition: transform 0.2s ease;
  display: inline-block;
}

.button-primary:hover {
  transform: scale(1.05);
}

.store-button {
  border: 1px solid #333;
  padding: 10px 20px;
  border-radius: 12px;
  text-decoration: none;
  color: white;
  margin: 0 10px;
  display: inline-flex;
  align-items: center;
  background: #161821;
}

/* Stats Grid */
.counter-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 40px;
  padding: 60px 0;
  border-top: 1px solid #1f2937;
}

.stat-item {
  text-align: center;
}

.stat-number {
  font-size: 3rem;
  color: var(--mint-green);
  margin-bottom: 5px;
}

.stat-label {
  color: var(--text-gray);
  text-transform: uppercase;
  letter-spacing: 1px;
  font-size: 0.8rem;
}

/* Images */
.hero-main-image {
  max-width: 100%;
  height: auto;
  margin-top: 60px;
  filter: drop-shadow(0 0 50px rgba(0, 214, 63, 0.1));
}
